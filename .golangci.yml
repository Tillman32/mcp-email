run:
  timeout: 5m
  tests: true

issues:
  exclude-dirs:
    - vendor
  exclude-files:
    - ".*\\.pb\\.go$"
    - ".*\\.gen\\.go$"
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
        - gosec
    - path: internal/mcp/server\.go
      text: "Error return value of .((os\\.stdout|os\\.stderr|os\\.Stdin)\\.(Read|Write|Close)|.*\\.Close|.*\\.Flush|os\\.(Remove|RemoveAll|Rename|Chmod|Chown|Chtimes)|.*\\.(Print|Printf|Println)|os\\.(Create|NewFile|Open|OpenFile)|.*\\.(Fprint|Fprintf|Fprintln))"
      linters:
        - errcheck
  max-issues-per-linter: 0
  max-same-issues: 0

linters-settings:
  errcheck:
    check-type-assertions: true
    check-blank: true
  goconst:
    min-len: 3
    min-occurrences: 2
  gofmt:
    simplify: true
  goimports:
    local-prefixes: github.com/Tillman32/mcp-email
  misspell:
    locale: US
  lll:
    line-length: 120
  dupl:
    threshold: 100
  gocyclo:
    min-complexity: 15
  maligned:
    suggest-new: true
  unconvert:
    suggestions: true
  unparam:
    check-exported: false
  nakedret:
    max-func-lines: 30
  prealloc:
    simple: true
    range-loops: true
    for-loops: false

linters:
  disable-all: true
  enable:
    - errcheck
    - goconst
    - gocritic
    - gofmt
    - goimports
    - gosec
    - gosimple
    - govet
    - ineffassign
    - misspell
    - nakedret
    - staticcheck
    - stylecheck
    - typecheck
    - unconvert
    - unparam
    - unused
    - whitespace

